import { AppShell } from '../components/layout/AppShell';

// Phase 4-এ এখানে আসল leaderboard data (win/loss stats) বসবে
export default function LeaderboardPage() {
  return (
    <AppShell>
      <h1 className="font-display text-2xl font-semibold text-parchment">লিডারবোর্ড</h1>
      <p className="mt-2 text-mist">এই অংশটা Phase 4-এ তৈরি হবে।</p>
    </AppShell>
  );
}
